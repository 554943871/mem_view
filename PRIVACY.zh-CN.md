# 隐私说明

memView 是一个本地 Markdown 记忆库只读查看器。

## 本地数据访问

memView 只读取你在应用中选择的本地文件夹。它会扫描 Markdown 文件，用来生成文件树、
提取文档标题，并检测 Mermaid 图。

## 不做网络同步

memView 不会把你的记忆库、文档内容、文件路径或设置上传到服务器。

当前应用没有遥测、数据分析、崩溃上报或账号登录。

## 本地设置

memView 会保存少量本地应用状态：

- 当前语言
- 上次选择的记忆库路径

这些设置保存在本地应用运行时中，可以在应用界面中调整。

## 只读行为

memView 设计为只读查看器。它不会编辑、创建、删除或移动所选记忆库中的文件。

## 发布说明

GitHub Release 会提供 macOS dmg 下载。如果构建产物没有使用 Apple Developer ID
签名并完成公证，macOS Gatekeeper 可能会在打开时提示警告。
